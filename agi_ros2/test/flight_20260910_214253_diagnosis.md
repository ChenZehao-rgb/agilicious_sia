# Jetson–本机分层运行日志诊断

来源：bags/flight_20260910_214253_147920，SQLite 只读查询并用 ROS 消息类型反序列化。
时间均为仿真秒；接收时间和消息内控制时间单独说明。

## 结论

自动接管从未成功，下降符合 AUTO 拒绝后 SITL 上锁的代码路径。
bag 不含实际 UDP 数据包及飞控 ARM 回读，因此上锁是基于代码和记录推断。
9221 条 control_command 的 permit_override 全部为 false；
29560 条 output_status 无 override_active=true，只有一个输出会话。

## 切换窗口

- 控制时间 72.424：IMU/状态年龄 13 ms，超过 10 ms，预热从 33 清零。
- 72.535：预热恢复到 12/50。
- /authority 在 72.540 置 AUTO 高。
- 首个 AUTO 高控制周期：控制时间 72.546，solve_seconds=0.011287280，
  超过 8 ms 截止时间；command_valid=false，controller_warm=false，permit_override=false。
  该耗时覆盖 HardwarePilot 的参考切换与控制流水线，不是纯 MPC 求解器计时。
- 后续状态年龄 14 ms、11 ms，控制周期耗时 9.99 ms，再次清零预热。
- RTK、配置验证等布尔标志在上述关键时刻正常；不能把这次归因为轨迹降高指令。

## 分布及实际运动

有效诊断样本 9221；状态/IMU 年龄中位数 2 ms、P95 13 ms、P99 26 ms、
最大 135 ms，715 个样本不符合 [0,10 ms] 的时间检查。
6740 个有限控制耗时样本：中位数 2.05 ms、P95 5.42 ms、P99 9.34 ms、最大 22.99 ms。
这些统计包含整段日志，不能单独归因于网络；包含数据传输、融合处理和调度影响。

真值高度：71.005 s 为 20.239 m；72.490 s 为 25.264 m；
72.985 s 达 26.107 m；75.460 s 落至 0.186 m。
记录的人工油门在 63.340 s 变为 1450，此后未变，AUTO 仅在 72.540 s 置高。
本次日志无法支持自动定点已成功的判断。

## 后续处理方向

保留本次原始 bag。先解决 Jetson 端的数据年龄与控制周期抖动，
让 AUTO=false 时控制状态持续就绪，再验证接管。
仅修改轨迹高度、UDP 地址或重切 AUTO 不能解决反复越过截止时间的问题。
若需要将 SITL 拒绝接管时的行为改为保留手动输入，应作为单独的控制权策略修改，
不能通过把 permit_override 强制置真处理。
本次诊断未修改控制或超时策略，也未重新运行飞行。
